// Big-O 

// O stands for order => used to describe the general shape of the growth curve 

// Ignoring coefficents, insignificant factors 

// O(1) (constant) , O(n) (linear), O(n^2) (quadratic)

// Best Case (big-omega)

// Average Case (big-theta)

// Worse Case (big-O) usually used as average case 
